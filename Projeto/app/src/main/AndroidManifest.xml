<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.fatec.projeto">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SobreActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MenuActivity"
            android:label="@string/title_activity_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ServicosActivity"
            android:parentActivityName=".MenuActivity" />
        <activity
            android:name=".BanhoTosaActivity"
            android:parentActivityName=".ServicosActivity" />
        <activity
            android:name=".AdestramentoActivity"
            android:parentActivityName=".ServicosActivity" />
        <activity
            android:name=".ServicosMedicosActivity"
            android:parentActivityName=".ServicosActivity" />
        <activity
            android:name=".HospedagemActivity"
            android:parentActivityName=".ServicosActivity" />
        <activity
            android:name=".AgendamentoActivity"
            android:parentActivityName=".MenuActivity"></activity>
    </application>

</manifest>